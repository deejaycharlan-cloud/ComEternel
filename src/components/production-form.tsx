'use client';
import {useState,useTransition} from 'react';
import {useRouter} from 'next/navigation';
import Link from 'next/link';
import {productionAction,type State} from '../app/production/actions';
export function ProductionForm({fields,children}:{fields:Record<string,string|number>;children:React.ReactNode}){const [state,setState]=useState<State>({status:'idle',message:''});const [pending,start]=useTransition();const router=useRouter();return <form onSubmit={e=>{e.preventDefault();const data=new FormData(e.currentTarget);const button=(e.nativeEvent as SubmitEvent).submitter as HTMLButtonElement|null;if(button?.name)data.set(button.name,button.value);start(async()=>{try{const result=await productionAction(state,data);setState(result);if(result.status==='success')router.refresh();}catch{setState({status:'error',message:'Connexion interrompue. Vos saisies sont conservées.'});}});}}>{Object.entries(fields).map(([name,value])=><input key={name} name={name} type="hidden" value={value}/>)}<fieldset disabled={pending||state.status==='success'} className="form-body">{children}</fieldset><p role={state.status==='error'?'alert':'status'}>{pending?'Enregistrement…':state.message}</p>{state.href&&<Link className="button" href={state.href}>Ouvrir →</Link>}</form>;}
