import {RightsFields} from './rights-fields';
import {contents,contentVersions,media} from '../db/production-schema';
export function ContentFields({item,version,assets,events}:{item?:typeof contents.$inferSelect;version?:typeof contentVersions.$inferSelect;assets:typeof media.$inferSelect[];events:{id:string;startDate:string;status:string}[]}){return <><label>Titre<input name="title" defaultValue={item?.title} required minLength={3} maxLength={180}/></label><div className="field-grid"><label>Canal<input name="channel" list="channels" defaultValue={item?.channel||'Facebook'} required/><datalist id="channels">{['Facebook','Instagram','TikTok','WhatsApp','Newsletter','Impression'].map(c=><option key={c}>{c}</option>)}</datalist></label><label>Format<input name="format" defaultValue={item?.format||'Message'} required minLength={2}/></label><label>Date de diffusion prévue<input name="plannedDate" type="date" defaultValue={item?.plannedDate} required/></label><label>Occurrence<select name="occurrenceId" defaultValue={item?.occurrenceId||''}><option value="">Projet / campagne</option>{events.filter(e=>e.status!=='cancelled').map(e=><option key={e.id} value={e.id}>{e.startDate}</option>)}</select></label></div><label>Objectif, public et brief<textarea name="brief" defaultValue={version?.brief} required minLength={5} maxLength={5000}/></label><label>Texte à diffuser (inclure les informations essentielles de l’affiche)<textarea name="caption" defaultValue={version?.caption} required minLength={5} maxLength={15000}/></label><label>Lien pratique<input name="practicalUrl" type="url" defaultValue={version?.practicalUrl}/></label><label>Lien Canva ou outil de création<input name="creationUrl" type="url" defaultValue={version?.creationUrl}/></label><label>Référence biblique / sources, si utilisées<textarea name="reference" defaultValue={version?.reference} maxLength={2000}/></label><fieldset><legend>Fichiers contrôlés de ce projet</legend>{assets.filter(m=>m.state==='controlled').map(m=><label className="check-label" key={m.id}><input type="checkbox" name="mediaIds" value={m.id} defaultChecked={version?.mediaIds.includes(m.id)}/>{m.name}</label>)}</fieldset><RightsFields value={version?.rights} channel={item?.channel}/><p>Vous pouvez conserver un brouillon sans compléter les droits. Ils seront obligatoires avant approbation.</p></>;}
