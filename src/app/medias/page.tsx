import Link from 'next/link';
import {programmeContext,MissingSpace,SpaceNavigation} from '../../modules/programme/context';
import {mediaService} from '../../modules/production/media-service';
import {programmeService} from '../../modules/programme/service';
import {Badge} from '../../components/ui/badge';
export const dynamic='force-dynamic';export const metadata={title:'Médias privés'};
const mediaLabels:Record<string,string>={uploading:'Envoi en cours',received:'Reçu · À examiner',controlled:'Contrôlé · Copie locale',quarantine:'Quarantaine'};
export default async function Page({searchParams}:{searchParams:Promise<{organisation?:string}>}){const {actor,db,org,spaces}=await programmeContext((await searchParams).organisation);if(!org)return <MissingSpace/>;const d=await mediaService(db).list(actor,org.id);const canEdit=(await Promise.all(d.projects.map(async p=>(await programmeService(db).get(actor,org.id,p.id)).canEdit))).some(Boolean);return <><section className="page-title"><p className="eyebrow">MÉDIAS · {org.name}</p><h1>Des fichiers retrouvables.</h1><p>Vos originaux restent dans le stockage privé. Les copies Drive se suivent dans les connexions de l’équipe.</p></section><SpaceNavigation spaces={spaces} selected={org.id} path="/medias"/><div className="quick-actions">{d.projects.some(p=>p.status==='preparation')&&<Link className="button" href={`/medias/importer?organisation=${org.id}`}>Importer des fichiers</Link>}{canEdit&&<Link className="button secondary" href={`/collectes?organisation=${org.id}`}>Collectes invitées</Link>}<Link className="button secondary" href={`/contenus?organisation=${org.id}`}>Contenus et livrables</Link></div><section className="card">{!d.media.length?<p>Aucun fichier reçu.</p>:<ul className="item-list">{d.media.map(m=><li key={m.id}><Link href={`/medias/${m.id}?organisation=${org.id}`}>{m.name}</Link><span>{d.projects.find(p=>p.id===m.projectId)?.title} · {(m.size/1024/1024).toFixed(1)} Mo</span><Badge tone={m.state==='quarantine'?'danger':m.state==='controlled'?'success':'warning'}>{mediaLabels[m.state]||m.state}</Badge>{m.proposalState==='pending'&&<Badge tone="warning">Proposition à classer</Badge>}{m.rights?.withdrawn&&<Badge tone="danger">Droits retirés</Badge>}</li>)}</ul>}</section></>;}
