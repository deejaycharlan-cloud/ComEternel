import Link from 'next/link';
import {mediaService} from '../../../modules/production/media-service';
import {programmeContext,MissingSpace} from '../../../modules/programme/context';
import {programmeService} from '../../../modules/programme/service';
import {Uploader} from '../../../components/uploader';
export const dynamic='force-dynamic';export const metadata={title:'Importer des fichiers'};
export default async function Page({searchParams}:{searchParams:Promise<{organisation?:string}>}){const {actor,db,org}=await programmeContext((await searchParams).organisation);if(!org)return <MissingSpace/>;const service=programmeService(db);const canCreate=await service.canCreate(actor,org.id);const targets=await mediaService(db).uploadTargets(actor,org.id);return <><section className="page-title"><h1>Importer les rushs d’un événement</h1><p>Choisissez l’événement et sa date, puis sélectionnez vos fichiers. Vous pouvez suspendre et reprendre un envoi avec le même fichier.</p></section><div className="quick-actions"><Link className="button secondary" href={`/medias?organisation=${org.id}`}>Les rushs des événements</Link><Link className="button secondary" href={`/aide?organisation=${org.id}#fichiers`}>Comment importer ?</Link></div>{targets.length?<section className="card"><Uploader destination="rush" organizationId={org.id} projects={targets} canCreateProject={canCreate}/></section>:<section className="card"><h2>{canCreate?'Créez d’abord votre événement':'Aucun événement accessible pour le moment'}</h2><p>{canCreate?'Les photos et vidéos sont rattachées à un événement pour retrouver leur date et leur contexte. Créez-en un, puis revenez importer vos fichiers.':'Demandez à votre administrateur de créer un événement en préparation. Vous pourrez ensuite déposer vos fichiers ici.'}</p>{canCreate&&<Link className="button" href={`/projets/nouveau?organisation=${org.id}`}>Créer un événement</Link>}</section>}</>;}
