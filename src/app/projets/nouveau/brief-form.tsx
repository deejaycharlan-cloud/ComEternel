'use client';
import { useState } from 'react';
import Link from 'next/link';
import { TimezoneField } from '../../../components/timezone-field';
import { ProgrammeForm } from '../../../components/programme-form';
import { eventTypes } from '../../../modules/programme/validation';
export function BriefForm({ org, people, creationKey }: { org: { id: string; name: string; timezone: string }; people: { id: string; name: string; email: string; permissions: string[] }[]; creationKey: string }) {
  const [kind, setKind] = useState('event'); const [allDay, setAllDay] = useState(true); const [cadence, setCadence] = useState('none');
  const person = (name: string, label: string) => <label>{label}<select name={name}><option value="">À désigner</option>{people.filter(p => name !== 'validatorId' || p.permissions.includes('content.validate')).map(p => <option key={p.id} value={p.id}>{p.name || p.email}</option>)}</select></label>;
  return <><section className="page-title"><p className="eyebrow">NOUVEAU PROJET · {org.name}</p><h1>Une intention, un programme.</h1><p>Enregistrez le projet et sa demande de communication. La demande reste « reçue » : aucune mission n’est engagée automatiquement.</p></section><Link href={`/projets?organisation=${org.id}`}>← Retour aux projets</Link><section className="card form-grid"><ProgrammeForm fields={{ operation: 'create', organizationId: org.id, creationKey }}><div className="field-grid"><label>Nom du projet<input name="title" required minLength={2} maxLength={120}/></label><label>Nature<select name="kind" value={kind} onChange={e => { setKind(e.target.value); if (e.target.value === 'campaign') { setCadence('none'); setAllDay(true); } }}><option value="event">Événement</option><option value="campaign">Campagne (sans événement)</option></select></label><label>Type / thème personnalisable<input name="eventType" list="event-types" required minLength={2} maxLength={100}/><datalist id="event-types">{eventTypes.map(t => <option value={t} key={t}/>)}</datalist></label><label>Ministère ou groupe (facultatif)<input name="ministry" maxLength={120}/></label><label>{allDay && kind === "event" ? "Date" : "Début"}<input name="startDate" type="date" required min="2000-01-01" max="2100-12-31"/></label>{(!allDay || kind === "campaign") && <label>Fin (incluse pour les journées entières)<input name="endDate" type="date" required min="2000-01-01" max="2100-12-31"/></label>}<TimezoneField label="Fuseau du projet" defaultValue={org.timezone}/><label>Horaires<select name="allDay" value={String(allDay)} onChange={e => setAllDay(e.target.value === 'true')} disabled={kind === 'campaign'}><option value="true">Journée(s) entière(s)</option><option value="false">Horaires précis</option></select>{kind === 'campaign' && <input type="hidden" name="allDay" value="true"/>}</label>{!allDay && <><label>Heure de début<input type="time" name="startTime" required/></label><label>Heure de fin<input type="time" name="endTime" required/></label></>}<label>Répétition<select name="cadence" value={cadence} onChange={e => setCadence(e.target.value)} disabled={kind === 'campaign'}><option value="none">Une seule fois</option><option value="weekly">Chaque semaine</option><option value="monthly">Chaque mois</option></select>{kind === 'campaign' && <input type="hidden" name="cadence" value="none"/>}</label>{cadence !== 'none' ? <label>Nombre d’occurrences<input name="occurrenceCount" type="number" min={1} max={104} defaultValue={4} required/></label> : <input type="hidden" name="occurrenceCount" value="1"/>}</div><p className="hint">Chaque occurrence conserve son identité. En récurrence mensuelle, un jour absent est ramené au dernier jour du mois. Les heures ambiguës lors d’un changement d’heure sont refusées.</p><div className="field-grid"><label>Lieu<input name="location" maxLength={300}/></label><label>Informations pratiques<textarea name="practicalInfo" rows={3} maxLength={2000}/></label>{person('ownerId','Responsable (facultatif)')}{person('validatorId','Validateur autorisé (facultatif)')}{person('deputyId','Remplaçant (facultatif)')}{person('decisionMakerId','Décideur de la demande (facultatif)')}<label>Niveau de communication<select name="communicationLevel"><option value="essential">Essentiel</option><option value="standard">Standard</option><option value="extended">Étendu</option></select></label><label>Date utile à la communication (facultative)<input name="usefulDate" type="date"/></label></div><h2>La demande de communication</h2><label>Objectif<textarea name="objective" required minLength={5} maxLength={2000}/></label><label>Public<input name="audience" required minLength={2} maxLength={500}/></label><label>Message<textarea name="message" required minLength={5} maxLength={3000}/></label><label>Ressources disponibles<textarea name="resources" maxLength={2000}/></label><div className="quick-actions"><button name="intent" value="preview">Prévisualiser les dates</button><button name="intent" value="save">Enregistrer le projet</button></div><p className="hint">Les champs restent en place si l’enregistrement échoue. Quitter la page avant confirmation perd les saisies.</p></ProgrammeForm></section></>;
}
