'use client';
import { useState } from 'react';
import { authClient } from '../../modules/identity/client';
import { Badge } from '../../components/ui/badge';
export function LoginForm({ googleEnabled, localMail, mailEnabled, linkError }: { googleEnabled: boolean; localMail: boolean; mailEnabled: boolean; linkError: boolean }) {
  const [email, setEmail] = useState(''); const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false);
  return <><section className="page-title"><p className="eyebrow">VOTRE COMPTE</p><h1>Bienvenue dans votre espace.</h1><p>Connectez-vous avec votre adresse personnelle. Rejoindre une association demande une invitation ; votre adresse seule n’accorde aucun accès.</p></section><div className="grid"><section className="card"><h2>Continuer avec mon adresse email</h2>{linkError && <p className="notice" role="alert">Ce lien est expiré, déjà utilisé ou invalide. Demandez un nouveau lien.</p>}<form onSubmit={async e => { e.preventDefault(); setBusy(true); setMessage(''); try { const { error } = await authClient.signIn.magicLink({ email, callbackURL: '/compte', errorCallbackURL: '/connexion?erreur=lien' }); setMessage(error ? 'Demande impossible pour le moment. Réessayez plus tard.' : (localMail ? 'Demande prise en compte. Consultez la boîte de test locale pour un éventuel lien, valable 5 minutes.' : 'Demande prise en compte. Consultez votre messagerie et vos courriers indésirables pour un éventuel lien, valable 5 minutes.')); } catch { setMessage('Connexion indisponible. Votre adresse est conservée ; réessayez.'); } finally { setBusy(false); } }}><label htmlFor="login-email">Votre adresse email</label><input id="login-email" type="email" autoComplete="email" required value={email} onChange={e => setEmail(e.target.value)}/><button disabled={busy || !mailEnabled}>{busy ? 'Demande en cours…' : 'Demander un lien de connexion'}</button><p role="status">{message}</p></form><hr/><h2>Continuer avec Google</h2>{googleEnabled ? <button disabled={busy} onClick={async () => { setBusy(true); try { const result = await authClient.signIn.social({ provider: 'google', callbackURL: '/compte' }); if (result.error) setMessage('Connexion Google indisponible.'); } catch { setMessage('Connexion Google indisponible.'); } finally { setBusy(false); } }}>Continuer avec Google</button> : <><Badge>Google non configuré</Badge><p>La connexion sera disponible lorsque les identifiants Google OAuth auront été configurés.</p></>}</section><aside className="card roadmap">{localMail ? <><Badge tone="warning">Essai local uniquement</Badge><h2>Une boîte de test sur cet ordinateur</h2><p>Aucun email ne part vers une vraie boîte. Les liens sont recueillis dans Mailpit pour essayer le parcours.</p>{localMail && <a className="button secondary" href="http://127.0.0.1:58025" target="_blank" rel="noreferrer">Ouvrir la boîte de test</a>}<p>Cette boîte permet d’essayer des identités fictives. N’utilisez pas de données sensibles dans cette version locale.</p></> : <><h2>Un lien personnel et temporaire</h2><p>Le lien expire après cinq minutes et ne peut être utilisé qu’une fois. Ne le transmettez à personne.</p></>}<p>Si un lien est expiré ou déjà utilisé, demandez-en un nouveau. Pour confirmer une opération d’administration, reconnectez-vous avec un nouveau lien.</p></aside></div></>;
}
