export async function signalClassification(){const url=process.env.N8N_DIRECT_DRIVE_WEBHOOK_URL,token=process.env.N8N_SERVICE_TOKEN;if(!url||!token)return;try{const u=new URL(url);if(u.protocol!=='https:')return;await fetch(u,{method:'POST',headers:{authorization:`Bearer ${token}`,'content-type':'application/json'},body:JSON.stringify({event:'drive.upload.completed'}),signal:AbortSignal.timeout(5000)});}catch{console.warn('drive_classification_signal_deferred');}}
